services:
  vaultwarden:
    image: vaultwarden/server:latest
    container_name: vaultwarden
    restart: unless-stopped
    network_mode: host
    environment:
      - ROCKET_PORT=8083
    volumes:
      - ./vw-data/:/data/
